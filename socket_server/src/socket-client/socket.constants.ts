import { ENV_CONFIG } from '../env';

export const USER_INITIATED_EVENT = "new-call";

export const USER_INITIATED_ROOM = ENV_CONFIG.USER_INITIATED_ROOM;

export const SOCKET_URL = ENV_CONFIG.SOCKET_URL;
